openapi: 3.1.0
info:
  title: raphael-slice
  version: 0.1.0
  description: Forking, slicing, lineage, attribution
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
